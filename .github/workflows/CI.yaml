name: CI

on:
  push:
    branches:
      - 'develop'

env:
  GO_VERSION: '1.20'
  CGO_ENABLED: 0
  IN_DOCKER: ""

jobs:

  build:
    runs-on: ubuntu-20.04

    steps:
      - name: Check out code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Build Step 1
        run: |
          sleep 15
          echo "step 1 success"
      - name: Build Step 2
        run: |
          sleep 15
          echo "step 2 success"

  test:
    runs-on: ubuntu-20.04
    needs:
      - build

    steps:
      - name: UAT
        run: |
          sleep 15
          echo "uat success"

      - name: SIT
        run: |
          sleep 15
          echo "sit success"

  deploy:
    runs-on: ubuntu-20.04
    needs:
      - test
    steps:
      - name: staging
        run: |
          sleep 15
          echo "staging success"

      - name: production
        run: |
          sleep 15
          echo "production success"
          exit 0

